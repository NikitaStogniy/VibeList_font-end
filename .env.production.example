# ============================================================================
# VibeList Frontend - PRODUCTION Environment Configuration
# ============================================================================
# This file contains production-specific settings
# Copy to .env.production and fill in your actual production values
# ============================================================================

# Sentry Configuration (Error Tracking)
# Get your DSN from: https://sentry.io/settings/projects/vibelist/keys/
EXPO_PUBLIC_SENTRY_DSN=https://YOUR-SENTRY-DSN@sentry.io/YOUR-PROJECT-ID

# API Configuration
# IMPORTANT: Replace with your production API URL
# Should match your backend deployment (AWS, Railway, Render, etc.)
EXPO_PUBLIC_API_URL=https://api.vibelist.cc/api/v1

# Environment
EXPO_PUBLIC_ENV=production

# OAuth Configuration
# Apple Sign-In Configuration (iOS)
# This should match your Apple Developer Services ID
EXPO_PUBLIC_APPLE_CLIENT_ID=com.stogniy.vibelist

# Feature Flags
EXPO_PUBLIC_ENABLE_ANALYTICS=true
EXPO_PUBLIC_ENABLE_DEBUG_MODE=false

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
#
# Before deploying to production, ensure:
#
# 1. API URL points to production backend (HTTPS only!)
# 2. Sentry DSN is configured for error tracking
# 3. Apple Client ID matches your Apple Developer account
# 4. Analytics are enabled (EXPO_PUBLIC_ENABLE_ANALYTICS=true)
# 5. Debug mode is disabled (EXPO_PUBLIC_ENABLE_DEBUG_MODE=false)
# 6. EAS project ID is set in app.json (already done: 6b61213c-81e0-474d-8990-f66599f0eccd)
# 7. Push notification certificates are configured in Expo dashboard
# 8. App store listings are created (iOS App Store, Google Play)
# 9. Privacy policy and terms of service URLs are added to app.json
# 10. All console.log statements are removed or gated behind __DEV__
#
# Build commands:
# - iOS:     eas build --platform ios --profile production
# - Android: eas build --platform android --profile production
# - Submit:  eas submit --platform ios --profile production
#
# ============================================================================
